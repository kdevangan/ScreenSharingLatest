<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cl_transparent"
    android:background="#80170E11"
    android:layout_height="match_parent">
    <TextView
        android:id="@+id/txScreenSharing"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:maxLines="1"
        android:layout_marginStart="15dp"
        android:ellipsize="end"
        app:layout_constraintEnd_toStartOf="@id/btn_stop_casting"
        android:text="@string/screen_is_visible"
        android:textColor="@color/white"
        />
    <Button
        android:id="@+id/btn_stop_casting"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:drawableLeft="@drawable/ic_baseline_cast_connected_24"
        android:drawableTint="@color/black"
        android:drawablePadding="8dp"
        android:layout_marginHorizontal="15dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@id/txScreenSharing"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:textAllCaps="true"
        android:text="STOP SHARING"/>


</androidx.constraintlayout.widget.ConstraintLayout>